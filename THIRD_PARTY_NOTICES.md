# Third-party notices

NEO Player Desktop uses Microsoft.Data.Sqlite, NAudio and TagLibSharp under their respective licenses. Distribution artifacts contain the .NET runtime when published self-contained. No FFmpeg or Vosk model binaries are bundled in the new build.

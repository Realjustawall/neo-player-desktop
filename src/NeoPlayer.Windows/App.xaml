<Application x:Class="NeoPlayer.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Resources/Strings.en.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
      <SolidColorBrush x:Key="NeoBackground" Color="#111216"/>
      <SolidColorBrush x:Key="NeoPanel" Color="#191B21"/>
      <SolidColorBrush x:Key="NeoForeground" Color="#F6F7FB"/>
      <SolidColorBrush x:Key="NeoMuted" Color="#9CA2B1"/>
      <SolidColorBrush x:Key="NeoAccent" Color="#FF8A00"/>
      <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource NeoBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource NeoForeground}"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
      </Style>
      <Style TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource NeoForeground}"/>
        <Setter Property="Background" Value="{DynamicResource NeoPanel}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="14,9"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource NeoForeground}"/>
        <Setter Property="Background" Value="{DynamicResource NeoPanel}"/>
        <Setter Property="BorderBrush" Value="#30333D"/>
        <Setter Property="Padding" Value="12,9"/>
      </Style>
      <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource NeoForeground}"/>
      </Style>
      <Style TargetType="ListBoxItem">
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="0,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
